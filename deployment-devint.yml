apiVersion: batch/v1
kind: CronJob
metadata:
  name: spring-batch-demo
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: spring-batch-demo
            image: spring-batch-demo:local
            imagePullPolicy: IfNotPresent
          restartPolicy: OnFailure